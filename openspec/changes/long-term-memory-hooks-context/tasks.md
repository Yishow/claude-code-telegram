## 1. 設定與資料模型

- [ ] 1.1 新增記憶功能設定欄位（`memory_system_plus`、記憶總開關、增強層總開關、四個 AI 子模組開關、增強模型預設 `gpt-5-mini`、timeout、召回上限）到 `Settings` 與 `.env.example`
- [ ] 1.2 設計並建立結構化記憶資料表/模型（類型、內容、優先度、TTL、來源訊息 ID、目錄範圍）
- [ ] 1.3 擴充 storage repository/facade 介面以支援記憶寫入、查詢、淘汰、衝突標記與 AI 模組設定持久化
- [ ] 1.4 定義 AI 模組設定作用範圍鍵（user_id + chat_id + thread_id）並建立查詢索引

## 2. Deterministic 基礎層

- [ ] 2.1 實作後置記憶萃取（從 prompt/response 產生事實、決策、待辦、偏好）
- [ ] 2.2 實作前置記憶召回（依目錄範圍、類型、時效、優先度過濾）
- [ ] 2.3 實作記憶組裝策略（注入格式、長度裁切、候選排序）
- [ ] 2.4 實作記憶淘汰與 TTL 清理流程，並保留淘汰原因

## 3. AI 增強層（預設開啟）

- [ ] 3.1 新增增強層服務，預設使用 `gpt-5-mini` 做記憶抽取品質提升與候選重排
- [ ] 3.2 實作四個 AI 子模組（extractor、reranker、conflict_detector、periodic_review）與執行管線
- [ ] 3.3 實作增強層 timeout、例外處理與回退策略（失敗立即改走 deterministic）
- [ ] 3.4 實作增強層與子模組運行時開關（可局部停用）

## 4. Telegram 互動切換控制

- [ ] 4.1 新增 Telegram 指令/按鈕入口，顯示 AI 模組狀態與可切換選項
- [ ] 4.2 實作 Telegram 端自由切換子模組開關並即時生效
- [ ] 4.3 實作 AI 設定 profile（例如 balanced/quality/fast）快速切換
- [ ] 4.4 實作切換結果回覆與錯誤處理（含權限/參數驗證）
- [ ] 4.5 實作作用範圍隔離（不同 chat/thread 設定互不覆蓋）
- [ ] 4.6 新增 `記憶系統+` 總開關控制（開啟/關閉）與狀態顯示

## 5. Hook 整合與主流程接線

- [ ] 5.1 在訊息送出前接入前置 hook，將記憶上下文組裝後再呼叫 provider
- [ ] 5.2 在回覆完成後接入後置 hook，保存本次互動對應記憶
- [ ] 5.3 讓 hook 執行遵循 AI 子模組開關（關閉即跳過）
- [ ] 5.4 實作「持久化設定 -> provider 執行參數」映射與不支援項目降級
- [ ] 5.5 實作 `memory_system_plus` 閘門：關閉時完全走原邏輯、開啟才進入記憶流程
- [ ] 5.6 確保 hook 任一步驟失敗時不阻斷主流程，並維持現有 session resume 行為
- [ ] 5.7 驗證不同工作目錄下的記憶隔離，避免跨專案污染

## 6. 可觀測性與安全控制

- [ ] 6.1 新增結構化觀測事件（命中數、淘汰數、淘汰原因、注入大小、回退原因）
- [ ] 6.2 新增增強層健康指標（延遲、失敗率、回退率）
- [ ] 6.3 新增 Telegram 切換審計事件（操作者、前後狀態、生效範圍）
- [ ] 6.4 新增 provider 降級事件（不支援參數、替代策略）
- [ ] 6.5 新增 `memory_system_plus` 開關前後基線比較指標
- [ ] 6.6 實作觀測資料脫敏規則，避免記錄敏感原文

## 7. 測試與驗證

- [ ] 7.1 新增 deterministic 層單元測試（抽取、召回、裁切、淘汰、衝突）
- [ ] 7.2 新增增強層測試（子模組開關、成功路徑、timeout、錯誤回退）
- [ ] 7.3 新增 `memory_system_plus=off` 回歸測試（驗證原邏輯等價）
- [ ] 7.4 新增 Telegram 切換測試（指令/按鈕、狀態查詢、profile、持久化）
- [ ] 7.5 新增整合測試（前置/後置 hook 接線、目錄隔離、主流程不中斷）
- [ ] 7.6 新增作用範圍測試（跨 chat/thread 不互相覆蓋）
- [ ] 7.7 建立優化測試基準（命中率、延遲、回退率）並定義調優門檻
- [ ] 7.8 執行驗證命令（目標 pytest、flake8）並確認無回歸
